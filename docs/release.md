# Release

Release checks:

1. `npm test`
2. `npm run pack:dry-run`
3. `npm run smoke`

Publishing should use npm trusted publishing and provenance where the package registry and repository settings support it.
