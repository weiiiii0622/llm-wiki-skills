# Package Plan Raw Notes

The CLI should install host-specific LLM wiki skills and keep generated vault files source-grounded.

Multi-file ingest should plan raw files before summarization so every source can be tracked through source summary, durable merge, and validation.
