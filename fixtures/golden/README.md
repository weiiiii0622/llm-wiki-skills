# Golden Fixtures

The test suite regenerates graph and health output from `fixtures/demo-vault` with `LLM_WIKI_SKILLS_NOW` pinned to a fixed timestamp. These fixtures are intentionally small enough to audit by hand and representative enough to catch graph schema drift.
